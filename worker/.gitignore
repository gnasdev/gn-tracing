node_modules/
# Local secrets for `wrangler dev` (never commit). Holds GOOGLE_CLIENT_SECRET.
.dev.vars
.dev.vars.*
# Keep the committed template that documents the required dev vars.
!.dev.vars.example
# Wrangler local state and build output.
.wrangler/
dist/
*.log
