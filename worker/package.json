{
  "name": "gn-tracing-oauth-proxy",
  "version": "1.7.15",
  "private": true,
  "description": "Cloudflare Worker multi-issuer OAuth token proxy (Google, Dropbox) for the GN Tracing extension.",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.19.0",
    "@cloudflare/workers-types": "^5.20260722.1",
    "@vitest/coverage-istanbul": "4.1.9",
    "typescript": "^5.7.0",
    "vitest": "4.1.9",
    "wrangler": "^4.115.0"
  }
}
