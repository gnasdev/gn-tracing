{
  "name": "gn-tracing",
  "version": "1.0.7",
  "private": true,
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "node esbuild.config.mjs --env development",
    "dist": "node esbuild.config.mjs --env production",
    "watch": "node esbuild.config.mjs --watch",
    "typecheck": "tsc --noEmit",
    "player:sync": "cd player-standalone && node scripts/sync-player.js",
    "player:dev": "cd player-standalone && npm run dev",
    "player:build": "cd player-standalone && npm run build",
    "player:dist": "cd player-standalone && npm run dist",
    "player:preview": "cd player-standalone && npm run preview",
    "player:deploy": "cd player-standalone && npm run deploy",
    "player:deploy:cloudflare": "cd player-standalone && npm run deploy:cloudflare",
    "build:all": "npm run build && npm run player:sync && npm run player:build",
    "dist:all": "npm run dist && npm run player:sync && npm run player:dist",
    "release:build": "npm run dist",
    "release:zip": "sh -c 'zip -r \"gn-tracing-extension-${GITHUB_REF_NAME:-local}.zip\" dist'",
    "release:crx": "node scripts/pack-release-crx.mjs",
    "release:ci": "npm run release:build && npm run release:zip && npm run release:crx",
    "watch:all": "concurrently 'npm run watch' 'npm run player:dev'"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "concurrently": "^8.2.2",
    "crx3": "^2.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}
